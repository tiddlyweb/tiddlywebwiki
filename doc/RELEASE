pre-release checklist

* update version number (in both setup.py and tiddlywebwiki package)
* update changelog
** review commit logs for both this repository and the client-side
   components at svn.tiddlywiki.org
* release:
    $ make release
    $ make bundle
